# Template for `op run --env-file=.env.template -- <command>`.
# Values are 1Password secret references, resolved at process start — never real
# secrets. Adjust the vault/item/field path to match your 1Password setup.
#
# Usage:
#   op run --env-file=.env.template -- agentlab "fix the failing test"
#   mise run agentlab -- "fix the failing test"   # wraps the above

OPENROUTER_API_KEY="op://Private/agentlab-openrouter/api key"
ANTHROPIC_API_KEY="op://Private/agentlab-anthropic/api key"
